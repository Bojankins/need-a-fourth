%div.row
  = simple_form_for @profile do |f|
    = f.input :description
    = f.input :gear
    = f.input :avatar, :as => :file, :multipart => true, :class => 'avatar'
    = f.submit "Update Your Profile", :class => 'button'
